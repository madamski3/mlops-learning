services:
  backend:
    image: ${LOCAL_IMAGE_NAME}
    ports:
      - "9999:9999"
    env_file:
      - ../.env
  kinesis:
    image: localstack/localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=kinesis
    env_file:
      - ../.env
